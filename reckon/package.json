{
  "name": "@builds/reckon",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json && tsc --noEmit -p tsconfig.app.json",
    "test": "vitest run",
    "score": "NODE_OPTIONS='--dns-result-order=ipv4first --no-network-family-autoselection' tsx --env-file=.env.local scripts/score.ts",
    "smoke": "NODE_OPTIONS='--dns-result-order=ipv4first --no-network-family-autoselection' tsx --env-file=.env.local scripts/smoke.ts",
    "dev": "NODE_OPTIONS='--dns-result-order=ipv4first --no-network-family-autoselection' next dev --port 3011",
    "build": "next build",
    "start": "next start --port 3011",
    "snapshot": "tsx scripts/snapshot.ts"
  },
  "dependencies": {
    "@builds/shared": "workspace:*",
    "@tursodatabase/serverless": "^1.4.0",
    "@typesafe-ai/sdk": "^0.6.0",
    "next": "16.3.5",
    "react": "19.2.8",
    "react-dom": "19.2.8"
  },
  "devDependencies": {
    "@rolldown/binding-linux-x64-gnu": "^1.2.9",
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "tsx": "^4.23.13",
    "vitest": "^5.0.1"
  }
}
