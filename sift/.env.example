# Copy to .env.local. Nothing real is committed.
# The deploy uses the SIFT_ prefix; locally the unprefixed names work too, and the prefixed one wins.
SIFT_TYPESAFE_API_KEY=
SIFT_TURSO_DATABASE_URL=
SIFT_TURSO_AUTH_TOKEN=
# The run's hard spend ceiling, in cents.
SIFT_CAP_CENTS=2500
