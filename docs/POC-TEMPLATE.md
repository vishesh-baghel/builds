# poc-NN · <workflow name>

> **Self-built experiment on synthetic data.** Every number below is measured from my own or
> synthetic data, never a client's. Client results appear only with written consent, named.

Status: **not started | building | shipped**

## The workflow

What the tedious version looks like, in the words of someone who does it.

## Why this one

The evidence that this workflow is worth automating, a source, not an assertion.

## Baseline (measured BEFORE the build)

| Metric | Baseline | After |
|---|---|---|
| | | |

A build without a pre-baselined number is a demo, not a result.

## Stack

| Layer | Choice |
|---|---|
| Orchestration | |
| Deploy target | |
| AI product(s) | |
| System of record | |

## Reliability

How this build behaves when things go wrong: what is idempotent, what retries, what escalates
to a human, what the audit log shows, what the spend cap is.

## Deliberately not doing

The boundaries. What a viewer might assume is automated but is not, and why.
