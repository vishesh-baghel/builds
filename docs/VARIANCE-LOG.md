# Variance log

What differed between builds and between engagements. Kept because the honest input to any
future decision about generalizing this work is recorded variance, not guessed abstraction.

Add a row whenever a build needs something the shared spine could not supply as-is, or whenever
two clients wanted the same workflow differently.

| Date | Build / client | What varied | Handled by | Worth generalizing? |
|---|---|---|---|---|
| 2026-09-19 | reckon | `Classified` carried one `label` and one `confidence`. The workflow's judgment is multi-label: 6 of the 72 scored replies genuinely carry two classes at once, and collapsing them to one manufactures a wrong answer. | Widened `Classified` in `shared` to carry per-class probabilities, the asserted set and a derived `primary`. | |
| 2026-09-19 | reckon | `PipelineOutcome` was `acted` **or** `escalated`, exclusively. A dispute must stop the chase **and** reach a person; a claimed payment must pause the chase **and** open a reconciliation item. | `Decision` now carries a list of actions plus an `escalate` flag, `act` is called once per action with its own idempotency key, and `runPipeline` returns both results and the optional escalation. | |
| 2026-09-19 | reckon | `log` was documented as the sixth stage but did not exist: `Pipeline` had five methods, `AuditEntry.stage` was a five-value union, and `runPipeline` took no `AuditLog`. The spine documented a pipeline it could not record. | `runPipeline` accepts an `AuditLog` and writes one row per stage that ran; the stage union gained `"log"`. | |
| 2026-09-20 | reckon | The PRD forbade free-text input on the sandbox outright, to stop a stranger running up a bill. In review the cost of that became clear: a demo that only replays committed fixtures reads as hardcoded, and a visitor who thinks it is faked has learned nothing true. | Free text accepted under a length cap, a committed-invoice requirement, the per-visitor allowance and the hard spend cap, and with no replay fallback, so it refuses rather than inventing a judgment. AC #35 rewritten rather than quietly dropped. | |
| 2026-09-20 | reckon | The PRD and README claimed Chaser's features page describes outbound reminders with inbound reply handling not listed. Checked properly against Chaser's own pages, that is wrong: its Gmail and Outlook integrations log replies against the customer, and its AI email generator reads a debtor's message and detects intent. | Claim corrected on all three surfaces and narrowed to what survives checking, the incumbent produces a draft for a person to send, this build produces a typed decision that moves chase state. Sources cited inline. | |
| 2026-09-20 | reckon | The PRD required persistent spend and idempotency stores on the deploy, calling the in-memory ones test-only. The deploy ships without them. | Measured first: a judgment costs 0.0084c, a visitor's whole monthly allowance costs 0.21c, and spending $1 takes about 11,900 calls. A per-instance ceiling is therefore enough, and the Turso adapter stays in place for the day it is not. Recorded rather than left as an unexplained gap between the spec and the deploy. | |
| 2026-09-23 | sift | The PRD routes an agency letter to "the principal and the coordinator". The per-topic plan gives each topic one owner, so one topic cannot fan out to two people. | An agency letter routes to the principal, who also receives every clock alert. The coordinator is not copied; the instrument's labels and the fixture README say so. | |
| 2026-09-23 | sift | The PRD's sandbox reads one scene firm. The dashboard design shows seven trades behind a firm switcher. | All seven render, from fixtures captioned illustrative. Only Meridian has an instrument, systems of record and code routing, so only Meridian can carry a measured number; the other six route from hand-written fixture defaults and publish nothing. | |
| 2026-09-23 | sift | The PRD asks for each class Noul's criteria to be drawn from the labelling rules. Run 1 shipped a generic yes/no pair per class; `other` fired on 16 of 18 vendor pitches and automation was 41%. | Criteria rewritten for Meridian from the class boundaries and labelling notes committed before run 1, nothing from its results; the clock question and every label left unchanged; the inbox run again. Run 1's judgments and scorecard stay committed in `runs/`, and the published scorecard recomputes its headline beside run 2's. | |
| 2026-09-23 | sift | The PRD requires the deploy's spend counter, per-visitor limit and 24-hour ceiling to persist and to be verified against the deployment (AC #36, #37). The deploy ships with live judging on and no Turso credentials, so all three are held per server instance and reset on a cold start. | Measured first: a live judgment costs about 0.005 cents, so even a full 500-call ceiling on one instance is about 2.5 cents a day, under a monthly cap of 2,500 cents per instance. The sidebar says "on this instance" while this holds. The Turso-backed store is built and tested and switches on when its two variables are set; AC #36 and #37 stay unticked until then. | |

## How to read this

- **Handled by**, a per-build implementation, a new option on a shared module, or a fork.
- **Worth generalizing?**, left blank until the same variance shows up a third time. Twice is
  a coincidence; three times is a requirement.
