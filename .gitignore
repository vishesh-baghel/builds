node_modules/
dist/
.next/
.turbo/
*.tsbuildinfo

# env and secrets — never commit
.env
.env.*
!.env.example
.dev.vars

# platform
.vercel/
.wrangler/

# editor / os
.DS_Store
.idea/
*.log

# loop run artifacts — the system is committed, its state is not
.claude/worktrees/
.claude/plans/
.claude/settings.local.json
.claude/.build
.claude/.gate-attempts

# /brag renders (brag plugin, enabled in .claude/settings.json)
brag-output/
brag-output-*/
